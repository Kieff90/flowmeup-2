---
phase: 02-build-landing-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/[lang]/layout.tsx
  - src/app/[lang]/page.tsx
  - src/lib/i18n.ts
  - src/types/i18n.ts
  - PRODUCT.md
  - DESIGN.md
autonomous: true
requirements:
  - TECH-01

must_haves:
  truths:
    - "Running `npm run dev` starts the Next.js 15 app without errors"
    - "Visiting localhost:3000 redirects to /it (default locale)"
    - "Visiting localhost:3000/en renders an English placeholder page"
    - "Tailwind v4 custom tokens (navy, amber, badge) are available in JSX classes"
    - "Inter font loads from Google Fonts with no layout shift"
    - "PRODUCT.md and DESIGN.md exist in project root (required by impeccable shape skill)"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15, Tailwind CSS v4, Framer Motion, TypeScript"
      contains: "next, tailwindcss, framer-motion"
    - path: "tailwind.config.ts"
      provides: "Custom color tokens (navy, amber, badge, brand, status)"
      exports: []
    - path: "src/app/[lang]/layout.tsx"
      provides: "Root layout with lang segment, Inter font, html lang attribute"
      exports: ["default"]
    - path: "src/lib/i18n.ts"
      provides: "Locale config: supported locales [it, en], default it, getDict function"
      exports: ["locales", "defaultLocale", "getDict", "Locale"]
    - path: "PRODUCT.md"
      provides: "Product context file required by impeccable shape skill"
    - path: "DESIGN.md"
      provides: "Design reference file required by impeccable shape skill"
  key_links:
    - from: "src/app/[lang]/layout.tsx"
      to: "src/lib/i18n.ts"
      via: "lang param to html[lang] attribute"
      pattern: "params\\.lang"
    - from: "src/app/layout.tsx"
      to: "/it"
      via: "redirect default locale"
      pattern: "redirect.*\\/it"
---

<objective>
Scaffold the Next.js 15 project with App Router, Tailwind CSS v4, Framer Motion, and the `/[lang]` routing segment for IT/EN i18n. This is the foundation every other plan depends on.

Purpose: Nothing in Wave 2 or later can proceed without a working scaffold. This plan creates the project structure, installs all dependencies, configures the design system tokens, and creates the two files (PRODUCT.md, DESIGN.md) required by the `impeccable shape` skill.

Output: Running Next.js 15 app at localhost:3000 with /it and /en routes, custom Tailwind tokens, Inter font loaded, TypeScript strict mode, and i18n routing working.
</objective>

<execution_context>
@/Users/mac-pedro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac-pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/MASTER.md
@.planning/phases/01-spec-and-concept/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore</files>
  <action>
    Invoke skill: `frontend-patterns` — use for idiomatic Next.js 15 App Router scaffold and Tailwind v4 integration.

    Run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes`

    Then install additional dependencies:
    ```
    npm install framer-motion
    npm install lucide-react
    npm install resend
    ```

    Configure `next.config.ts`:
    - No experimental flags needed for App Router in Next 15
    - Add `reactStrictMode: true`
    - Keep default image optimization settings

    Configure `tsconfig.json` strict mode:
    - `"strict": true`
    - `"noUncheckedIndexedAccess": true`

    Verify package.json contains: `"next": "^15.*"`, `"framer-motion"`, `"lucide-react"`, `"resend"`.
  </action>
  <verify>
    <automated>cd /Users/mac-pedro/Desktop/DEV/flowmeup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build completes without errors. next, framer-motion, lucide-react, resend all in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 tokens and globals</name>
  <files>tailwind.config.ts, src/app/globals.css</files>
  <action>
    Replace `tailwind.config.ts` with the exact config from `design-system/MASTER.md §9` — copy the full TypeScript config block verbatim. The `content` array must cover `./src/**/*.{js,ts,jsx,tsx,mdx}`.

    In `src/app/globals.css`:
    1. Import Inter from Google Fonts:
       ```css
       @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
       ```
    2. Import Tailwind directives (`@tailwind base; @tailwind components; @tailwind utilities;`)
    3. Add CSS custom properties from MASTER.md §9 (`:root` block with all semantic tokens)
    4. Add `prefers-reduced-motion` override:
       ```css
       @media (prefers-reduced-motion: reduce) {
         * {
           animation-duration: 0.01ms !important;
           animation-iteration-count: 1 !important;
           transition-duration: 0.01ms !important;
         }
       }
       ```
    5. Add `.reveal` utility class:
       ```css
       .reveal {
         opacity: 0;
         transform: translateY(20px);
         transition: opacity 400ms ease-out, transform 400ms ease-out;
       }
       .reveal.visible {
         opacity: 1;
         transform: translateY(0);
       }
       ```

    Do NOT add dark mode variants. MASTER.md §8: light mode only for v1.
  </action>
  <verify>
    <automated>cd /Users/mac-pedro/Desktop/DEV/flowmeup && npm run build 2>&1 | grep -i "error" | head -10 || echo "No errors"</automated>
  </verify>
  <done>Tailwind config exports all navy, amber, badge, brand, status tokens. globals.css loads Inter font. `prefers-reduced-motion` override present. `.reveal` class defined.</done>
</task>

<task type="auto">
  <name>Task 3: Implement /[lang] routing, i18n types, PRODUCT.md and DESIGN.md</name>
  <files>
    src/app/layout.tsx,
    src/app/[lang]/layout.tsx,
    src/app/[lang]/page.tsx,
    src/lib/i18n.ts,
    src/types/i18n.ts,
    PRODUCT.md,
    DESIGN.md
  </files>
  <action>
    Invoke skill: `frontend-patterns` — idiomatic Next.js App Router i18n with `/[lang]` segment.

    **`src/types/i18n.ts`** — type contracts first:
    ```typescript
    export type Locale = 'it' | 'en'
    export type Dict = {
      // Populated in Wave 2 when sections are built
      [key: string]: string | Dict
    }
    ```

    **`src/lib/i18n.ts`**:
    ```typescript
    import type { Locale } from '@/types/i18n'

    export const locales: Locale[] = ['it', 'en']
    export const defaultLocale: Locale = 'it'

    export async function getDict(locale: Locale): Promise<Record<string, unknown>> {
      // Dictionaries will be added in Wave 2 when sections are built
      // Returns empty object until section plans populate /src/dictionaries/
      try {
        const dict = await import(`@/dictionaries/${locale}.json`)
        return dict.default
      } catch {
        return {}
      }
    }
    ```

    **`src/app/layout.tsx`** — root layout, redirect to default locale:
    ```typescript
    import { redirect } from 'next/navigation'
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Flowmeup — AI Sales Agents',
      description: 'AI agents for field sales teams',
    }

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      redirect('/it')
    }
    ```

    **`src/app/[lang]/layout.tsx`** — lang-scoped layout:
    ```typescript
    import type { Metadata } from 'next'
    import { locales, defaultLocale } from '@/lib/i18n'
    import type { Locale } from '@/types/i18n'
    import { notFound } from 'next/navigation'
    import '../globals.css'

    export async function generateStaticParams() {
      return locales.map((lang) => ({ lang }))
    }

    export default async function LangLayout({
      children,
      params,
    }: {
      children: React.ReactNode
      params: Promise<{ lang: string }>
    }) {
      const { lang } = await params
      if (!locales.includes(lang as Locale)) notFound()

      return (
        <html lang={lang}>
          <body className="font-sans antialiased bg-slate-50 text-slate-900">
            {children}
          </body>
        </html>
      )
    }
    ```

    **`src/app/[lang]/page.tsx`** — placeholder, replaced in Wave 2:
    ```typescript
    import type { Locale } from '@/types/i18n'

    export default async function HomePage({
      params,
    }: {
      params: Promise<{ lang: Locale }>
    }) {
      const { lang } = await params
      return (
        <main>
          <p className="p-8 text-navy-900 font-bold">
            Flowmeup — {lang.toUpperCase()} — scaffold ready
          </p>
        </main>
      )
    }
    ```

    **`PRODUCT.md`** — required by `impeccable shape` skill:
    Write a concise product context file describing Flowmeup: AI sales agents for Italian SMB field sales teams. Core value: 90-second lead logging from phone while driving. 4 agents (Voice Lead LIVE, Lead Scout LIVE, Pre-Call Brief COMING SOON, Follow-Up Radar COMING SOON). Done-for-you model. Target audience: Italian/EU SMB owners, construction/manufacturing/distribution sectors.

    **`DESIGN.md`** — required by `impeccable shape` skill:
    Write a concise design reference pointing to `design-system/MASTER.md` as the single source of truth. Include: navy (#0F2349) primary, amber (#F59E0B) CTA, Inter font, 8px grid, 8 sections with their background colors from MASTER.md §Quick Reference. Light mode only.
  </action>
  <verify>
    <automated>cd /Users/mac-pedro/Desktop/DEV/flowmeup && npm run dev &amp; sleep 5 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/it &amp;&amp; curl -s -o /dev/null -w " %{http_code}" http://localhost:3000/en; kill %1 2>/dev/null</automated>
  </verify>
  <done>localhost:3000/it returns 200. localhost:3000/en returns 200. PRODUCT.md and DESIGN.md exist in project root. i18n.ts exports locales, defaultLocale, getDict.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/mac-pedro/Desktop/DEV/flowmeup
npm run build
# Must complete without TypeScript errors
# Must show /it and /en routes in output
```
Check files exist:
- `PRODUCT.md` (root)
- `DESIGN.md` (root)
- `src/lib/i18n.ts`
- `tailwind.config.ts` with navy/amber tokens
- `src/app/globals.css` with `.reveal` class and `prefers-reduced-motion` override
</verification>

<success_criteria>
1. `npm run build` completes with 0 errors
2. `/it` and `/en` routes exist and return 200
3. `tailwind.config.ts` contains all navy, amber, badge tokens from MASTER.md §9 (verbatim)
4. `globals.css` imports Inter, defines `.reveal` class, includes `prefers-reduced-motion` override
5. `PRODUCT.md` and `DESIGN.md` exist in project root (impeccable shape skill prerequisite)
6. `src/lib/i18n.ts` exports `locales`, `defaultLocale`, `getDict`, `Locale`
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-landing-page/02-01-SUMMARY.md` following the summary template.
</output>
