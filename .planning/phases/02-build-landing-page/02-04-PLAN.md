---
phase: 02-build-landing-page
plan: "04"
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/components/sections/Delivery.tsx
  - src/components/sections/Pricing.tsx
  - src/components/sections/ContactForm.tsx
  - src/components/layout/LanguageSwitcher.tsx
  - src/app/[lang]/page.tsx
  - src/lib/actions/submitForm.ts
autonomous: true
requirements:
  - LAND-06
  - LAND-07
  - LAND-08
  - DSGN-04
  - CONV-01
  - CONV-02
  - CONV-03

must_haves:
  truths:
    - "Delivery section shows 4 numbered steps: 30-min call, Configuration, Team onboarding, Live"
    - "Pricing section shows pricing table with all 4 agents (LIVE priced, COMING SOON in muted grey)"
    - "Discount callout shows: -10% for 2 agents, -15% for 3, -20% for 4"
    - "Contact form has exactly 5 fields: Nome/Name, Azienda/Company, Ruolo/Role, Settore/Sector, Messaggio/Message"
    - "Form submit button says 'Inviami il tuo processo' (IT) / 'Send us your process' (EN)"
    - "After successful form submit, inline success message appears (no page redirect)"
    - "Calendar link renders as secondary CTA: 'Oppure scegli direttamente un orario' / 'Or pick a time directly'"
    - "Language switcher renders IT/EN links in navbar, switching from /it to /en and vice versa"
    - "CTA and Contact section have navy-900 background"
  artifacts:
    - path: "src/components/sections/Delivery.tsx"
      provides: "Section 6 — 4-step delivery process, slate-100 background"
      exports: ["Delivery"]
    - path: "src/components/sections/Pricing.tsx"
      provides: "Section 7 — pricing table with 4 agents and discount callout"
      exports: ["Pricing"]
    - path: "src/components/sections/ContactForm.tsx"
      provides: "Section 8 — 5-field form + calendar link + inline success state"
      exports: ["ContactForm"]
    - path: "src/components/layout/LanguageSwitcher.tsx"
      provides: "IT/EN switcher using Next.js Link to /it and /en"
      exports: ["LanguageSwitcher"]
    - path: "src/lib/actions/submitForm.ts"
      provides: "Next.js Server Action for form submission via Resend or Formspree"
      exports: ["submitForm"]
  key_links:
    - from: "src/components/sections/ContactForm.tsx"
      to: "src/lib/actions/submitForm.ts"
      via: "form action prop"
      pattern: "submitForm"
    - from: "src/components/layout/LanguageSwitcher.tsx"
      to: "/it | /en"
      via: "Next.js Link href"
      pattern: "href.*\\/it|href.*\\/en"
---

<objective>
Build Sections 6–8 (Delivery, Pricing, Contact form), implement the language switcher, and wire the contact form backend. This completes all 8 landing sections and the full conversion flow.

Purpose: Delivery and Pricing lead the visitor to the contact point. The form is the primary conversion mechanism. Language switching must work correctly for the bilingual page.

Output: Complete 8-section landing page (all sections wired in page.tsx), working language switcher, functional form with inline success state, calendar link. Page is complete and functional at this point.
</objective>

<execution_context>
@/Users/mac-pedro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac-pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@design-system/MASTER.md
@.planning/phases/01-spec-and-concept/COPY-BRIEF-IT.md
@.planning/phases/01-spec-and-concept/COPY-BRIEF-EN.md
@.planning/phases/02-build-landing-page/02-02-SUMMARY.md
@.planning/phases/02-build-landing-page/02-03-SUMMARY.md

<interfaces>
<!-- From src/components/ui/Button.tsx (02-02) -->
```typescript
// Primary button: amber-500, CTA style
// Secondary: navy border/text on light
// secondary-on-dark: white border on navy bg
<Button variant="primary" | "secondary" | "secondary-on-dark" href="..." fullWidth>
```

<!-- From src/lib/i18n.ts -->
```typescript
export type Locale = 'it' | 'en'
export const locales: Locale[]
```

<!-- From src/app/[lang]/page.tsx — current structure -->
// Navbar + Hero + Problem + Solution + AgentCards + Differentiator already present
// This plan appends: Delivery + Pricing + ContactForm + Footer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Delivery, Pricing sections and extend dictionaries</name>
  <files>
    src/components/sections/Delivery.tsx,
    src/components/sections/Pricing.tsx,
    src/dictionaries/it.json,
    src/dictionaries/en.json
  </files>
  <action>
    Invoke skill: `impeccable craft layout` — layout for step-based Delivery and table-based Pricing.

    **`src/components/sections/Delivery.tsx`**:
    - Background: slate-100 (#F1F5F9)
    - Padding: py-12 md:py-16 lg:py-20
    - H2: standard section heading pattern
    - Sub-copy: `text-base lg:text-lg text-slate-600 mt-3 mb-10`
    - 4 steps horizontal (desktop) / vertical (mobile):
      `grid grid-cols-1 md:grid-cols-4 gap-6 mt-8`
    - Each step: step number circle (navy-900 bg, white text, 40px, font-bold), step title, 1-line description
    - Step data (from COPY-BRIEF):
      1. "Chiamata 30 min" / "30-min call" — "Descrivi come lavora il tuo team"
      2. "Configurazione" / "Configuration" — "Noi configuriamo gli agenti"
      3. "Onboarding team" / "Team onboarding" — "30 minuti per il tuo team"
      4. "Operativo" / "Live" — "Dal contratto al primo agente: una settimana"
    - Bold "una settimana" / "one week" in the last step description using `<strong>`
    - `.reveal` class on H2; step items with `.reveal` for Wave 3 stagger

    **`src/components/sections/Pricing.tsx`**:
    - Background: slate-50 (#F8FAFC)
    - Padding: py-12 md:py-16 lg:py-20
    - H2: standard pattern
    - Setup fee line: `text-lg font-semibold text-slate-700 mt-3`
    - Table: CSS Grid `grid grid-cols-[2fr_1fr_1fr]` on desktop, `grid grid-cols-[2fr_1fr]` on mobile
    - Headers: Agent name | Monthly price | Status (hidden on mobile)
    - Row data (4 rows):
      - Voice Lead | €59/mese | LIVE — row uses Badge component (live variant)
      - Lead Scout | €99/mese | LIVE
      - Pre-Call Brief | €69/mese | COMING SOON — use `text-slate-400` for price (muted)
      - Follow-Up Radar | €49/mese | COMING SOON — muted
    - Price number: `text-2xl font-extrabold text-navy-900` (LIVE rows) or `text-2xl font-extrabold text-slate-300` (COMING SOON rows)
    - Row border: `border-b border-slate-200`, last row no border
    - Row hover: `hover:bg-slate-50` (already on slate-50 — use `hover:bg-slate-100`)
    - Discount callout box: `bg-amber-50 border border-amber-200 rounded-xl p-4 mt-6`
      Content: -10% 2 agenti, -15% 3 agenti, -20% 4 agenti
      Example: azienda edile, Voice Lead + Follow-Up Radar = €97.20/mese
    - CTA button below table: secondary variant, links to `#contact`
    - `.reveal` class on H2; table rows with `.reveal` for Wave 3 stagger
    - NOTE: Tone is conversational — no "Buy now", no countdown (pricing not yet validated via discovery calls)

    **Extend `src/dictionaries/it.json` and `src/dictionaries/en.json`**:
    Add `delivery` and `pricing` keys. Use exact copy from COPY-BRIEF-IT.md §6–7 and COPY-BRIEF-EN.md §6–7.
  </action>
  <verify>
    <automated>cd /Users/mac-pedro/Desktop/DEV/flowmeup && npx tsc --noEmit 2>&1 | head -20 || echo "Clean"</automated>
  </verify>
  <done>Delivery section shows 4 steps. Pricing table shows 4 agent rows with correct prices. COMING SOON prices are visually muted. Discount callout renders with amber tint. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build ContactForm, LanguageSwitcher, wire complete page</name>
  <files>
    src/components/sections/ContactForm.tsx,
    src/components/layout/LanguageSwitcher.tsx,
    src/lib/actions/submitForm.ts,
    src/app/[lang]/page.tsx
  </files>
  <action>
    Invoke skill: `frontend-patterns` — Next.js Server Actions for form, Link-based i18n switcher pattern.
    Invoke skill: `impeccable craft` — Section 8 ContactForm with navy background.

    **`src/lib/actions/submitForm.ts`** — Server Action:
    ```typescript
    'use server'

    export type FormData = {
      name: string
      company: string
      role: string
      sector: string
      message: string
      locale: 'it' | 'en'
    }

    export type SubmitResult = {
      success: boolean
      error?: string
    }

    export async function submitForm(data: FormData): Promise<SubmitResult> {
      // Use Resend if RESEND_API_KEY is set, else fall back to Formspree
      // For v1: use Formspree (zero-backend, no RESEND_API_KEY needed during build)
      // POST to https://formspree.io/f/{FORMSPREE_ID} or use NEXT_PUBLIC_FORMSPREE_ID env var
      // If neither env var set: log to console and return success: true (dev fallback)
      try {
        const endpoint = process.env.FORMSPREE_ENDPOINT
        if (!endpoint) {
          console.log('[submitForm] No endpoint configured. Form data:', data)
          return { success: true }
        }
        const res = await fetch(endpoint, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        })
        if (!res.ok) throw new Error(`HTTP ${res.status}`)
        return { success: true }
      } catch (err) {
        return { success: false, error: err instanceof Error ? err.message : 'Unknown error' }
      }
    }
    ```

    **`src/components/sections/ContactForm.tsx`**:
    - Background: navy-900 (#0F2349) — visual closure matching hero
    - Padding: py-12 md:py-16 lg:py-20
    - `id="contact"` on the section (anchor target for all CTA buttons)
    - H2: `text-2xl md:text-3xl font-bold text-white leading-[1.2]`
    - Sub-copy: `text-base lg:text-lg text-navy-300 mt-3 mb-8`
    - Layout: `grid grid-cols-1 md:grid-cols-2 gap-12` — form left, calendar link right
    - Form fields (5 fields, each with label + input/textarea):
      1. name — `<input type="text">` — label: "Nome" / "Name"
      2. company — `<input type="text">` — label: "Azienda" / "Company"
      3. role — `<input type="text">` — label: "Ruolo" / "Role"
      4. sector — `<input type="text">` — label: "Settore" / "Sector"
      5. message — `<textarea rows={4}>` — label: "Messaggio" / "Message"
         Placeholder IT: "Es. Abbiamo 4 commerciali in campo, usiamo Excel per i lead, WhatsApp per comunicare."
         Placeholder EN: "E.g. We have 4 field reps, we use Excel for leads, WhatsApp for team communication."
    - All inputs: use exact MASTER.md §5 Form Input classes — `border-slate-200`, amber focus ring, 16px font (prevents iOS zoom)
    - Labels: `text-sm font-semibold text-slate-300` (adapted for dark background)
    - Submit button: primary variant (amber), full-width on mobile, `type="submit"`
    - Button text IT: "Inviami il tuo processo" | EN: "Send us your process"
    - Client component (`'use client'`) for form state management:
      - `useState` for `status: 'idle' | 'submitting' | 'success' | 'error'`
      - On submit: call `submitForm()`, update status
      - Success state: hide form, show inline message:
        IT: "Ricevuto. Ti scriviamo entro 24 ore per confermare l'orario."
        EN: "Got it. We'll write to you within 24 hours to confirm a time."
      - Error state: show inline error, re-enable submit button
    - Calendar link (right column / below form on mobile):
      `<a href="#" target="_blank" rel="noopener noreferrer">` (placeholder # — user will configure Calendly URL)
      Text IT: "Oppure scegli direttamente un orario"
      Text EN: "Or pick a time directly"
      Style: `text-amber-400 underline underline-offset-4 hover:text-amber-300`
      Add `// TODO: Replace # with Calendly URL` comment
    - Add `src/dictionaries/it.json` and `en.json` entries for `contact` key

    **`src/components/layout/LanguageSwitcher.tsx`**:
    ```typescript
    'use client'
    import Link from 'next/link'
    import type { Locale } from '@/types/i18n'

    export function LanguageSwitcher({ currentLang }: { currentLang: Locale }) {
      return (
        <div className="flex items-center gap-2 text-sm">
          <Link
            href="/it"
            className={currentLang === 'it'
              ? 'text-white font-bold underline underline-offset-4'
              : 'text-navy-300 hover:text-white transition-colors'}
          >
            IT
          </Link>
          <span className="text-navy-400">|</span>
          <Link
            href="/en"
            className={currentLang === 'en'
              ? 'text-white font-bold underline underline-offset-4'
              : 'text-navy-300 hover:text-white transition-colors'}
          >
            EN
          </Link>
        </div>
      )
    }
    ```
    Update Navbar to replace the placeholder IT/EN toggle with `<LanguageSwitcher currentLang={lang} />`.

    **Update `src/app/[lang]/page.tsx`** — complete the page:
    Import and add Delivery, Pricing, ContactForm, Footer:
    ```typescript
    <main>
      <Hero dict={dict} />
      <Problem dict={dict} />
      <Solution dict={dict} />
      <AgentCards dict={dict} lang={lang} />
      <Differentiator dict={dict} />
      <Delivery dict={dict} />
      <Pricing dict={dict} lang={lang} />
      <ContactForm dict={dict} lang={lang} />
    </main>
    <Footer dict={dict} />
    ```
  </action>
  <verify>
    <automated>cd /Users/mac-pedro/Desktop/DEV/flowmeup && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Build passes. All 8 sections render. Contact form has 5 fields with correct labels. Submit button text correct per locale. LanguageSwitcher shows active locale bold. Calendar link placeholder renders. Success state component is wired to submitForm action.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/mac-pedro/Desktop/DEV/flowmeup
npm run build
# Expect: 0 errors, /it and /en routes complete

# Manual verification targets:
# /it#contact: form labels in Italian, submit button "Inviami il tuo processo"
# /en#contact: form labels in English, submit button "Send us your process"
# Navbar: IT | EN switcher, active locale bold
# Pricing: 4 rows, COMING SOON prices muted, discount callout present
# Delivery: 4 numbered steps, "una settimana" / "one week" emphasized
```
</verification>

<success_criteria>
1. All 8 sections render in the correct order (Hero→Problem→Solution→Agents→Differentiator→Delivery→Pricing→Contact)
2. Contact section has `id="contact"` — all CTA buttons on page anchor to it
3. Form has exactly 5 fields: name, company, role, sector, message
4. Submit button text is "Inviami il tuo processo" (IT) / "Send us your process" (EN)
5. Form shows inline success message on submit (no redirect)
6. LanguageSwitcher renders correctly with active locale underlined
7. Calendar link is present (# placeholder with TODO comment)
8. Section backgrounds follow MASTER.md Quick Reference: Delivery=slate-100, Pricing=slate-50, Contact=navy-900
9. Build passes with 0 TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-landing-page/02-04-SUMMARY.md` following the summary template.
</output>
